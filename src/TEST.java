public class TEST {
    //system quiz
    //show to student the question related to the matiere faible
    //show the answer of question exactly 
    // background instruction to increment the score and other?

    //1_
    /*
        function show quiz(matier){
            name : matier.name
            anneeScolaire : matiere.anneeScolaire
            trimestre : matiere.trimestre

            PrepareStatement pstmtExercice
            PrepareStatement pstmtQuestion
            PrepareStatement pstmtAnswer
            ArrayList <Exercice>exos = new ArrayList<>();
            exoSQL ="SELECT * where matiereName ='name' anneeScolaire LIMIT()"
            ResultSet rs = pstmtExercice.excuteQuery(); //have the specific exercices
            Exercice tmpExo;
            while(rs.next){
                tmpExo.setId(rs.get(1));
                tmpExo.setmatierName
            }
            // we have exo object infos
            
        }
     */
    
}
